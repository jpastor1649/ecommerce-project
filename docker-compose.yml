services:
  auth-postgres:
    image: postgres:16-alpine
    container_name: ecommerce_auth_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${AUTH_POSTGRES_USER}
      POSTGRES_PASSWORD: ${AUTH_POSTGRES_PASSWORD}
      POSTGRES_DB: ${AUTH_POSTGRES_DB}
    ports:
      - "${AUTH_POSTGRES_EXTERNAL_PORT:-5433}:5432"
    volumes:
      - auth_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${AUTH_POSTGRES_USER} -d ${AUTH_POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  user-postgres:
    image: postgres:16-alpine
    container_name: ecommerce_user_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${USER_POSTGRES_USER}
      POSTGRES_PASSWORD: ${USER_POSTGRES_PASSWORD}
      POSTGRES_DB: ${USER_POSTGRES_DB}
    ports:
      - "${USER_POSTGRES_EXTERNAL_PORT:-5432}:5432"
    volumes:
      - user_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${USER_POSTGRES_USER} -d ${USER_POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: ecommerce_rabbitmq
    restart: unless-stopped
    volumes:
      - ./rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf:ro
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 40s

  auth-service:
    build:
      context: ./backend
      dockerfile: auth_service/Dockerfile
    container_name: ecommerce_auth_service
    restart: unless-stopped
    depends_on:
      auth-postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    environment:
      DATABASE_URL: ${AUTH_DATABASE_URL}
      APP_NAME: ${AUTH_APP_NAME:-Auth Service}
      DEBUG: ${AUTH_DEBUG:-false}
      JWT_SECRET: ${AUTH_JWT_SECRET}
      JWT_ALGORITHM: ${AUTH_JWT_ALGORITHM:-HS256}
      JWT_EXPIRE_MINUTES: ${AUTH_JWT_EXPIRE_MINUTES:-60}
      RABBITMQ_HOST: ${RABBITMQ_HOST:-rabbitmq}
      USER_PROFILE_QUEUE_NAME: ${USER_PROFILE_QUEUE_NAME:-user_profile_create_queue}
    command: uvicorn auth_service.src.main:app --host 0.0.0.0 --port 8001
    ports:
      - "${AUTH_SERVICE_EXTERNAL_PORT:-8001}:8001"

  user-service:
    build:
      context: ./backend/user-service
      dockerfile: Dockerfile
    container_name: ecommerce_user_service
    restart: unless-stopped
    depends_on:
      user-postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    environment:
      DATABASE_URL: ${USER_DATABASE_URL}
      RABBITMQ_HOST: ${RABBITMQ_HOST:-rabbitmq}
      USER_PROFILE_QUEUE_NAME: ${USER_PROFILE_QUEUE_NAME:-user_profile_create_queue}
    ports:
      - "${USER_SERVICE_EXTERNAL_PORT:-8002}:8000"

  product-service:
    build:
      context: ./backend
      dockerfile: product_service/Dockerfile
    container_name: ecommerce_product_service
    restart: unless-stopped
    depends_on:
      user-postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: ${PRODUCT_DATABASE_URL:-postgresql+asyncpg://user_user:user_password@user-postgres:5432/user_db}
      APP_NAME: ${PRODUCT_APP_NAME:-Product Service}
      DEBUG: ${PRODUCT_DEBUG:-false}
      JWT_SECRET: ${AUTH_JWT_SECRET}
      JWT_ALGORITHM: ${AUTH_JWT_ALGORITHM:-HS256}
    command: uvicorn product_service.main:app --host 0.0.0.0 --port 8003
    ports:
      - "${PRODUCT_SERVICE_EXTERNAL_PORT:-8003}:8003"

  api-gateway:
    image: nginx:1.27-alpine
    container_name: ecommerce_api_gateway
    restart: unless-stopped
    depends_on:
      auth-service:
        condition: service_started
      user-service:
        condition: service_started
      product-service:
        condition: service_started
    ports:
      - "${API_GATEWAY_EXTERNAL_PORT:-8000}:8000"
    volumes:
      - ./api-gateway/nginx.conf:/etc/nginx/conf.d/default.conf:ro

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        HTTP_PROXY: ${HTTP_PROXY:-}
        HTTPS_PROXY: ${HTTPS_PROXY:-}
        NO_PROXY: ${NO_PROXY:-}
    container_name: ecommerce_frontend
    restart: unless-stopped
    depends_on:
      api-gateway:
        condition: service_started
    environment:
      VITE_API_URL: ${VITE_API_URL:-http://localhost:8000}
    ports:
      - "${FRONTEND_EXTERNAL_PORT:-3000}:3000"

volumes:
  auth_postgres_data:
  user_postgres_data:
